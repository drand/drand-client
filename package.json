{
  "name": "@alanshaw/drand-client",
  "version": "0.0.5",
  "description": "A client to the drand randomness beacon network.",
  "type": "module",
  "main": "./drand.js",
  "scripts": {
    "test": "ava -v -T 10m",
    "lint": "standard",
    "build": "npm run build:wasm",
    "build:wasm": "cd wasm && GOOS=js GOARCH=wasm go build -ldflags=\"-s -w\" -o drand.wasm main.go"
  },
  "author": "Alan Shaw",
  "license": "MIT",
  "devDependencies": {
    "abort-controller": "^3.0.0",
    "ava": "^3.9.0",
    "node-fetch": "^2.6.0",
    "standard": "^14.3.4"
  },
  "dependencies": {}
}
